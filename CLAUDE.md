# KUMU

酒販店の店員向け接客支援ツール。店内カメラで来店者の動きを追い、店内マップ上に「誰がどこで迷っているか」と「その人の好みの推定」を表示する。

## なぜ作るのか

消費者は自分の好みを言語化する語彙を持っていない。だから店頭では「辛口ください」としか言えず、店員はそこから深掘りできない。

一方で、店内での滞留や回遊は、本人が言葉にできない好みを表している。**KUMU は行動を味の語彙に翻訳し、店員に「声をかけるきっかけ」と「第一声の中身」を同時に渡す。**

この一行がすべての設計判断の基準になる。迷ったらここに戻る。

## 体制とリポジトリ構成

2名。フロントエンド1名、バックエンド1名。

```
/frontend   店員向け Web アプリ（React + Vite + TypeScript）
/backend    API サーバー
それぞれのディレクトリ配下にticketsがある。
```

各ディレクトリの CLAUDE.md に、その領域固有の規約がある。**作業前に必ず読むこと。**

## API 契約

フロントとバックの境界。ここを勝手に変えない。変更が必要なら実装せず、まず人間に確認する。

### POST /map — マップの保存

```json
{
  "grid": { "width": 10, "height": 8 },
  "objects": [
    {
      "type": "shelf",
      "id": "shelf-a",
      "name": "棚A",
      "x": 2, "y": 1,
      "length": 3,
      "facing": "north",
      "brand_ids": [1234, 5678]
    },
    { "type": "entrance", "id": "ent-1", "x": 0, "y": 7 },
    { "type": "register", "id": "reg-1", "x": 9, "y": 7 }
  ]
}
```

- `x`, `y` は起点セル（左上）
- `facing` が `north` / `south` なら棚は x 方向に、`east` / `west` なら y 方向に `length` 分伸びる
- `entrance` と `register` は 1 セル固定。`length` と `facing` を持たない
- グリッドは 10×8 固定

### GET /map — マップの読み戻し

POST と同じ形を返す。

### GET /sessions — 来店者リスト

フロントが 10 秒間隔でポーリングする。

```json
[
  {
    "session_id": "a1b2",
    "x": 3, "y": 2,
    "state": "hesitating",
    "dwell_sec": 42,
    "shelf_id": "shelf-a"
  }
]
```

- `state` は `moving` / `viewing` / `hesitating` の 3 値
- `shelf_id` は**バックエンドが隣接判定した結果**。フロントは判定しない

## ドメイン用語

| 用語 | 意味 |
|---|---|
| 棚 (shelf) | 商品が並ぶ什器。1×2 / 1×3 / 1×4 のいずれか。**表裏（facing）を持つ** |
| facing | 棚が向いている方角。客はこの側からしか棚を見られない |
| セッション | 1 人の来店者の 1 回の滞在。**退店で消滅する** |
| 迷い (hesitating) | 同じ棚の前に一定時間とどまっている状態。接客の合図 |
| 4タイプ分類 | 日本酒を香りの高さ×味わいの濃淡で分けた分類。薫酒 / 爽酒 / 醇酒 / 熟酒 |
| さけのわ | 銘柄と味の特徴を提供する外部 API。6 軸のフレーバー値を持つ |

### 棚の表裏について

棚Aの裏側に立っている人は、棚Aを見ていない。背中合わせに置かれた棚を正しく扱うために `facing` がある。

判定範囲は**棚が向いている側の隣接セルのみ**。4 近傍ではない。

## 全体の禁止事項

- **顔の特徴量を保存しない。** 個人を識別しない。来店者は毎回新しい匿名セッションとして扱う
- **リピーター識別の機能を追加しない。** 意図的にスコープ外にしている
- **さけのわデータを使う画面には帰属表示を入れる。** 利用規約上の必須要件
- **エラーコードの数字をそのまま画面に出さない。** 利用者は IT に不慣れな店主

## 未合意の事項

以下はまだ決まっていない。**勝手に決めて実装しない。** 遭遇したら TODO コメントを残し、人間に確認する。

- POST 失敗時に実際に返るステータスコード（特に 403 の実体）
- 通路の両側から向かい合う棚の扱い（`shelf_ids` 配列にするか、最も近い 1 つか）
- `brand_ids` にさけのわの ID をそのまま使うか、内部 ID を振るか

## 作業の進め方

- チケットは `frontend or backend /tickets/` にある。**1 セッションで 1 チケット**を原則とする
- チケットの「スコープ外」に書かれたことをやらない。先回りしない
- 完了条件をすべて満たしてから完了とする
